meta {
  name: Логін адміністратора
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@news.com",
    "password": "admin-password"
  }
}
